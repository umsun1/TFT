package com.tft.batch.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {
    // Spring Boot Auto-configuration for StringRedisTemplate handles everything default
}
